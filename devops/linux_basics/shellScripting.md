shebang

as all shell scripts files have the same extenstion .sh

so to differentiate that which shell the os should use -> we do 

#!/bin/sh -> for sh shell
#!/bin/zzh -> for zzh shell
#!/bin/bash -> for the bash shell

let me again note this 

shell is a program that interpretes the commands we write in terminal


to execture a shell script -> ./nameofscript.sh or bash nameofscript.sh
